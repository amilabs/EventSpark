/**
 * Sample project configuration.
 * Copy this file to config.js, make changes, and remove initial "return true".
 */

return true;

aServices = {
    'SERVICE': {
        type: 'meteor',
        host: 'meteor.address:3000',
        user: 'admin@meteor.address',
        pass: 'password'
    }
};

aIntervals = {
    meteor: 10000
}